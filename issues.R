- 1 tree
- ineccesible literature
- use natural logarithm, but state that it is log10
- un-transformed latitude, longitude for distance matrix
- ​ one additional population where DPRtrans = YES but obs_time = NA so who knows about that one

table(b$obs_time[b$DPRtrans == 'YES'])
table(b$period_orig,b$DPRtrans)